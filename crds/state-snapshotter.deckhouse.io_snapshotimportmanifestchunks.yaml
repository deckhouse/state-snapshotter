---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: snapshotimportmanifestchunks.state-snapshotter.deckhouse.io
spec:
  group: state-snapshotter.deckhouse.io
  names:
    kind: SnapshotImportManifestChunk
    listKind: SnapshotImportManifestChunkList
    plural: snapshotimportmanifestchunks
    shortNames:
    - simchunk
    singular: snapshotimportmanifestchunk
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.importRequestName
      name: Request
      type: string
    - jsonPath: .spec.nodeId
      name: NodeID
      type: string
    - jsonPath: .spec.index
      name: Index
      type: integer
    - jsonPath: .spec.objectsCount
      name: Objects
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              data:
                description: |-
                  Data contains the gzipped JSON array of objects, base64 encoded.
                  Format: base64(gzip(json[]))
                  The controller re-encodes this into the canonical ManifestCheckpointContentChunk format.
                maxLength: 1048576
                type: string
              importRequestName:
                description: ImportRequestName is the name of the owning SnapshotImportRequest.
                minLength: 1
                type: string
              index:
                description: Index is the 0-based index of this chunk within the node's
                  manifest set.
                type: integer
              nodeId:
                description: NodeID identifies which node in the import tree this
                  chunk belongs to.
                minLength: 1
                type: string
              objectsCount:
                description: ObjectsCount is the number of objects in this chunk's
                  data.
                type: integer
              total:
                description: Total is the total number of chunks for this node's manifest
                  set.
                minimum: 1
                type: integer
            required:
            - data
            - importRequestName
            - index
            - nodeId
            - objectsCount
            - total
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
