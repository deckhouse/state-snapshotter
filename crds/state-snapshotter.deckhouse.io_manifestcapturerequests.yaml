---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
    module: state-snapshotter
  name: manifestcapturerequests.state-snapshotter.deckhouse.io
spec:
  group: state-snapshotter.deckhouse.io
  names:
    kind: ManifestCaptureRequest
    listKind: ManifestCaptureRequestList
    plural: manifestcapturerequests
    shortNames:
    - mcr
    singular: manifestcapturerequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.checkpointName
      name: Checkpoint
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              targets:
                description: |-
                  Targets specifies the objects to capture. It MUST contain at least one target — at minimum the
                  snapshotted object's own manifest. A single-object domain snapshot passes its own source identity,
                  and the namespace-root aggregator always includes its own Namespace object, so a well-formed capture
                  is never empty. An empty (or omitted) target set is a contract violation: the SDK fails closed with
                  ErrEmptyManifest before creating the request, and the CRD rejects it via the spec-level CEL rule
                  above ("has(self.targets) && size(self.targets) > 0").
                  All targets must be namespaced objects in the same namespace as the ManifestCaptureRequest, with a
                  single exception: the capture's own Namespace object (core v1 Namespace whose name equals the
                  ManifestCaptureRequest namespace) is the only allowed cluster-scoped target.
                items:
                  properties:
                    apiVersion:
                      description: APIVersion of the target object
                      type: string
                    kind:
                      description: |-
                        Kind of the target object
                        Cluster-scoped resources (Node, PersistentVolume, ClusterRole, etc.) are NOT allowed, with a single
                        exception: the core v1 Namespace whose name equals the ManifestCaptureRequest namespace (the capture's
                        own Namespace object).
                      type: string
                    name:
                      description: Name of the target object
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
            type: object
            x-kubernetes-validations:
            - message: spec.targets must list at least one object to capture (at minimum
                the snapshotted object's own manifest)
              rule: has(self.targets) && size(self.targets) > 0
          status:
            properties:
              checkpointName:
                description: CheckpointName is the name of the cluster-scoped ManifestCheckpoint
                type: string
              completionTimestamp:
                description: CompletionTimestamp is when the request was completed
                format: date-time
                type: string
              conditions:
                description: |-
                  Conditions represent the latest available observations of the request state
                  Only Ready condition is used - it is set to True on success or False on final failure
                  Ready condition is set only in final state (terminal success or terminal failure)
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
