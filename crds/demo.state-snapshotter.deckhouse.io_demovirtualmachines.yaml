---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: demovirtualmachines.demo.state-snapshotter.deckhouse.io
spec:
  group: demo.state-snapshotter.deckhouse.io
  names:
    kind: DemoVirtualMachine
    listKind: DemoVirtualMachineList
    plural: demovirtualmachines
    shortNames:
    - demovm
    singular: demovirtualmachine
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DemoVirtualMachine is a minimal placeholder "resource" side for
          CSD mapping (PR5b); not reconciled beyond registration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            type: object
          status:
            type: object
        type: object
    served: true
    storage: true
