---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: demovirtualmachinesnapshots.demo.state-snapshotter.deckhouse.io
spec:
  group: demo.state-snapshotter.deckhouse.io
  names:
    kind: DemoVirtualMachineSnapshot
    listKind: DemoVirtualMachineSnapshotList
    plural: demovirtualmachinesnapshots
    shortNames:
    - demovmsnap
    singular: demovirtualmachinesnapshot
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DemoVirtualMachineSnapshot is an intermediate demo snapshot node
          (PR5b) under root NamespaceSnapshot; disk snapshots may attach here.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DemoVirtualMachineSnapshotSpec defines the desired state
              of DemoVirtualMachineSnapshot.
            properties:
              rootNamespaceSnapshotRef:
                description: RootNamespaceSnapshotRef identifies the NamespaceSnapshot
                  run root (same namespace allowed).
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              virtualMachineName:
                description: 'VirtualMachineName is a logical VM identifier for this
                  demo snapshot (PR5b: identity only).'
                type: string
            required:
            - rootNamespaceSnapshotRef
            type: object
          status:
            description: DemoVirtualMachineSnapshotStatus defines the observed state
              of DemoVirtualMachineSnapshot.
            properties:
              boundSnapshotContentName:
                description: BoundSnapshotContentName is the cluster-scoped DemoVirtualMachineSnapshotContent
                  name, once created.
                type: string
              childrenSnapshotRefs:
                description: ChildrenSnapshotRefs lists child snapshot objects (e.g.
                  DemoVirtualDiskSnapshot) under this VM snapshot.
                items:
                  description: |-
                    NamespaceSnapshotChildRef identifies one child NamespaceSnapshot in the N2b manifests-only tree
                    (element of status.childrenSnapshotRefs). It is not a generic snapshot reference: it does not
                    carry apiVersion/kind; N2b currently assumes children are NamespaceSnapshot roots in the named
                    namespace. A future multi-kind child model would require extending or replacing this shape.
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
