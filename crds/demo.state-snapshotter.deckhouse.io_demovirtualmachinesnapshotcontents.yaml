---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: demovirtualmachinesnapshotcontents.demo.state-snapshotter.deckhouse.io
spec:
  group: demo.state-snapshotter.deckhouse.io
  names:
    kind: DemoVirtualMachineSnapshotContent
    listKind: DemoVirtualMachineSnapshotContentList
    plural: demovirtualmachinesnapshotcontents
    shortNames:
    - demovmsnapc
    singular: demovirtualmachinesnapshotcontent
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DemoVirtualMachineSnapshotContent is cluster-scoped content for
          a DemoVirtualMachineSnapshot (PR5b).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DemoVirtualMachineSnapshotContentSpec defines the desired
              state of DemoVirtualMachineSnapshotContent.
            properties:
              snapshotRef:
                description: SnapshotRef points to the namespaced DemoVirtualMachineSnapshot.
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - snapshotRef
            type: object
          status:
            description: DemoVirtualMachineSnapshotContentStatus defines the observed
              state of DemoVirtualMachineSnapshotContent.
            properties:
              childrenSnapshotContentRefs:
                description: ChildrenSnapshotContentRefs lists child snapshot content
                  objects (e.g. DemoVirtualDiskSnapshotContent).
                items:
                  description: |-
                    NamespaceSnapshotContentChildRef identifies one child NamespaceSnapshotContent in the N2b graph
                    (element of status.childrenSnapshotContentRefs). Cluster-scoped name only; kind is implied
                    (NamespaceSnapshotContent). Not a universal SnapshotContent reference.
                  properties:
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
              manifestCheckpointName:
                description: ManifestCheckpointName points to the materialized manifest
                  checkpoint for this content node.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
