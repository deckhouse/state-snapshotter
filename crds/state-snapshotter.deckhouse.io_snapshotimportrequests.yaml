---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: snapshotimportrequests.state-snapshotter.deckhouse.io
spec:
  group: state-snapshotter.deckhouse.io
  names:
    kind: SnapshotImportRequest
    listKind: SnapshotImportRequestList
    plural: snapshotimportrequests
    shortNames:
    - sir
    singular: snapshotimportrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.rootSnapshotName
      name: Snapshot
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              nodes:
                description: |-
                  Nodes describes the snapshot tree to be recreated, ordered leaf-to-root.
                  Each entry mirrors a NodeRecord from the archive index.
                items:
                  properties:
                    apiVersion:
                      description: APIVersion of the snapshot object to create (e.g.
                        "state-snapshotter.deckhouse.io/v1alpha1").
                      minLength: 1
                      type: string
                    children:
                      description: Children lists the IDs of immediate child nodes.
                      items:
                        type: string
                      type: array
                    hasData:
                      description: HasData is true when this node has volume data
                        that needs to be imported via VCR.
                      type: boolean
                    id:
                      description: ID is the stable node identifier from the archive
                        (matches NodeRecord.id).
                      minLength: 1
                      type: string
                    kind:
                      description: Kind of the snapshot object to create (e.g. "Snapshot",
                        "DemoVirtualMachineSnapshot").
                      minLength: 1
                      type: string
                    name:
                      description: Name is the desired name for the snapshot object.
                      minLength: 1
                      type: string
                    parentId:
                      description: ParentID is the ID of the parent node, or empty
                        for the root node.
                      type: string
                  required:
                  - apiVersion
                  - hasData
                  - id
                  - kind
                  - name
                  type: object
                type: array
              rootSnapshotName:
                description: RootSnapshotName is the name of the root Snapshot to
                  be created in the request namespace.
                minLength: 1
                type: string
              storageClassMapping:
                additionalProperties:
                  type: string
                description: |-
                  StorageClassMapping optionally remaps storage class names from the archive
                  to target cluster storage classes (source -> target).
                type: object
              ttl:
                description: |-
                  TTL is the retention duration for the root ObjectKeeper (e.g. "168h").
                  Empty means no TTL (keep until explicit deletion).
                type: string
              volumes:
                description: |-
                  Volumes lists per-volume staging PVC references populated by the CLI
                  after DataImport materialises each volume into a PVC.
                items:
                  properties:
                    nodeId:
                      description: NodeID references the ImportNode.ID this volume
                        belongs to.
                      minLength: 1
                      type: string
                    pvcName:
                      description: PVCName is the name of the original PersistentVolumeClaim
                        (from the archive).
                      minLength: 1
                      type: string
                    stagingPvcName:
                      description: |-
                        StagingPVCName is the name of the PVC populated by DataImport on the target cluster.
                        The CLI sets this field after DataImport completes; the controller waits for it before
                        creating the VolumeCaptureRequest.
                      type: string
                    volumeMode:
                      description: 'VolumeMode is the volume mode: "Block" or "Filesystem".'
                      enum:
                      - Block
                      - Filesystem
                      type: string
                  required:
                  - nodeId
                  - pvcName
                  - volumeMode
                  type: object
                type: array
            required:
            - nodes
            - rootSnapshotName
            type: object
          status:
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the request state.
                  Expected condition types: Ready.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdSnapshotName:
                description: CreatedSnapshotName is the name of the root Snapshot
                  object that was created.
                type: string
              nodeProgress:
                description: NodeProgress contains per-node status summary.
                items:
                  properties:
                    id:
                      description: ID is the node identifier.
                      type: string
                    message:
                      description: Message provides a human-readable status message.
                      type: string
                    phase:
                      description: Phase is the per-node phase.
                      type: string
                  required:
                  - id
                  type: object
                type: array
              phase:
                description: Phase is a high-level summary of progress.
                enum:
                - Pending
                - Importing
                - Ready
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
