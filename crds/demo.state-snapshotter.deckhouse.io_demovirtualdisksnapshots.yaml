---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: demovirtualdisksnapshots.demo.state-snapshotter.deckhouse.io
spec:
  group: demo.state-snapshotter.deckhouse.io
  names:
    kind: DemoVirtualDiskSnapshot
    listKind: DemoVirtualDiskSnapshotList
    plural: demovirtualdisksnapshots
    shortNames:
    - demovdsnap
    singular: demovirtualdisksnapshot
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DemoVirtualDiskSnapshot is a minimal demo snapshot node (PR5a).
          Wires into root Snapshot via children*Refs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DemoVirtualDiskSnapshotSpec defines the desired state of
              DemoVirtualDiskSnapshot.
            properties:
              dataSource:
                description: |-
                  DataSource, when set, switches this disk snapshot into IMPORT mode: instead of capturing the live
                  DemoVirtualDisk (no MCR/VCR planning), the data leg is materialized by the common controller from
                  the referenced DataImport's produced artifact (DataImport.status.dataArtifactRef). The domain
                  controller skips capture planning for import-mode snapshots; manifests + children arrive via the
                  per-CR manifests-and-children-refs-upload payload. Immutable once set (C5; demo-domain reference
                  for the domain leaf spec.dataSource->DataImport import trigger).
                properties:
                  name:
                    description: Name is the DataImport object name in the same namespace
                      as this snapshot.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: spec.dataSource is immutable
                  rule: self == oldSelf
              sourceRef:
                description: |-
                  SourceRef identifies the DemoVirtualDisk captured by this snapshot. It is the single
                  source-of-truth for what the snapshot captures (both manually-created and root-planned
                  snapshots) and is immutable once set. On import it still carries the original disk identity
                  (the live disk may be absent); spec.dataSource is what switches the node into import mode.
                properties:
                  apiVersion:
                    minLength: 1
                    type: string
                  kind:
                    minLength: 1
                    type: string
                  name:
                    minLength: 1
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
                x-kubernetes-validations:
                - message: spec.sourceRef is immutable
                  rule: self == oldSelf
            required:
            - sourceRef
            type: object
          status:
            description: DemoVirtualDiskSnapshotStatus defines the observed state
              of DemoVirtualDiskSnapshot.
            properties:
              boundSnapshotContentName:
                description: BoundSnapshotContentName is the cluster-scoped SnapshotContent
                  name, once created.
                type: string
              conditions:
                description: Conditions report readiness (e.g. Ready=True for generic
                  parent children-readiness aggregation).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dataCaptured:
                description: |-
                  DataCaptured is set by the common controller once this disk snapshot's data leg has been durably
                  handed off to SnapshotContent (dataRefs published and the VolumeSnapshotContent owned by the content).
                  Domain-only suppression signal: the domain controller reads it to stop re-creating the VCR after the
                  common controller deletes it. Always considered captured for a manifest-only disk (no data leg).
                type: boolean
              manifestCaptureRequestName:
                description: ManifestCaptureRequestName is the temporary MCR owned
                  by this snapshot while own-scope capture runs.
                type: string
              manifestCaptured:
                description: |-
                  ManifestCaptured is set by the common controller once this snapshot's manifest capture has been
                  durably handed off to SnapshotContent (manifestCheckpointName published and the ManifestCheckpoint
                  owned by the content). It is a domain-only suppression signal: the domain controller reads it to
                  stop re-creating the MCR after the common controller deletes it, without ever reading SnapshotContent.
                type: boolean
              volumeCaptureRequestName:
                description: |-
                  VolumeCaptureRequestName is the temporary VCR owned by this disk snapshot while data-leg capture runs.
                  The common controller reads this VCR's result to enrich and publish SnapshotContent.status.dataRefs;
                  the domain controller never touches SnapshotContent itself.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
