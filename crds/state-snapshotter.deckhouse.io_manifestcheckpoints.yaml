---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: manifestcheckpoints.state-snapshotter.deckhouse.io
spec:
  group: state-snapshotter.deckhouse.io
  names:
    kind: ManifestCheckpoint
    listKind: ManifestCheckpointList
    plural: manifestcheckpoints
    shortNames:
    - mcp
    singular: manifestcheckpoint
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.totalObjects
      name: Objects
      type: integer
    - jsonPath: .status.totalSizeBytes
      name: Size
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              manifestCaptureRequestRef:
                description: ManifestCaptureRequestRef references the ManifestCaptureRequest
                  that created this checkpoint
                properties:
                  name:
                    description: Name of the referred object
                    type: string
                  namespace:
                    description: Namespace of the referred object (empty for cluster-scoped
                      objects)
                    type: string
                  uid:
                    description: UID of the referred object
                    type: string
                required:
                - name
                - namespace
                - uid
                type: object
              sourceNamespace:
                description: SourceNamespace is the namespace of the original ManifestCaptureRequest
                type: string
            required:
            - manifestCaptureRequestRef
            - sourceNamespace
            type: object
          status:
            properties:
              chunks:
                description: Chunks contains information about all chunks
                items:
                  properties:
                    checksum:
                      description: |-
                        Checksum is the SHA256 hash of the compressed chunk data (base64 encoded)
                        Used for integrity validation and debugging
                      type: string
                    index:
                      description: Index of the chunk (0-based)
                      type: integer
                    name:
                      description: Name of the chunk
                      type: string
                    objectsCount:
                      description: ObjectsCount is the number of objects in this chunk
                      type: integer
                    sizeBytes:
                      description: SizeBytes is the size of this chunk in bytes (compressed)
                      format: int64
                      type: integer
                  required:
                  - index
                  - name
                  - objectsCount
                  - sizeBytes
                  type: object
                type: array
              conditions:
                description: |-
                  Conditions represent the latest available observations of the checkpoint state
                  Expected condition types: Ready
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              totalObjects:
                description: TotalObjects is the total number of objects captured
                type: integer
              totalSizeBytes:
                description: TotalSizeBytes is the total size of all chunks in bytes
                  (compressed)
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
