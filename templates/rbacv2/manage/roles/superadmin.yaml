apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: d8:manage:state-snapshotter:superadmin
  labels:
    heritage: deckhouse
    module: state-snapshotter
    rbac.deckhouse.io/use-role: user
    rbac.deckhouse.io/kind: manage
    rbac.deckhouse.io/level: subsystem
    rbac.deckhouse.io/subsystem: state-snapshotter
    rbac.deckhouse.io/aggregate-to-all-as: superadmin
aggregationRule:
  clusterRoleSelectors:
    - matchLabels:
        rbac.deckhouse.io/kind: manage
        rbac.deckhouse.io/aggregate-to-state-snapshotter-as: superadmin
rules: []
