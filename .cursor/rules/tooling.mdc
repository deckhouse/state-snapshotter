---
description: Allowed-without-confirmation: codegen and API tests. Rest → see api-codegen, api-change-checklist, controller-redeploy-and-remote-e2e.
alwaysApply: true
---

# Tooling

Allowed without confirmation: `bash hack/generate_code.sh`; `cd api && go test ./...`. When to run codegen, linter, e2e: see api-codegen.mdc, api-change-checklist.mdc, controller-redeploy-and-remote-e2e.mdc. **`./go-lint.sh`:** CI parity via `GO_BUILD_TAGS`; commit vs lint order and WIP without the wrapper — **go-lint-script.mdc** (the script does **not** run `git checkout -f`).
