---
description: Allowed-without-confirmation: codegen and API tests. Rest → see api-codegen, api-change-checklist, controller-redeploy-and-remote-e2e.
alwaysApply: true
---

# Tooling

Allowed without confirmation: `bash hack/generate_code.sh`; `cd api && go test ./...`. When to run codegen, linter, e2e: see api-codegen.mdc, api-change-checklist.mdc, controller-redeploy-and-remote-e2e.mdc. **`./go-lint.sh`:** паритет с CI через `GO_BUILD_TAGS`; порядок commit vs lint и WIP без обёртки — **go-lint-script.mdc** (скрипт **не** делает `git checkout -f`).
